tandards: 0.2

ame: Backup networkid secret key
osts: polkadot,cumulus,encointer
ather_facts: True
asks:
 - name: Restart default_service
   become: True
   become_user: root
   ansible.builtin.systemd:
     name: "{{ default_service }}"
     state: restarted

