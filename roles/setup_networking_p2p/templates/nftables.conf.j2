#!/usr/sbin/nft -f
# /etc/nftables.conf
# Managed by ansible: roles/setup_networking_p2p

flush ruleset

# Dual-stack mangle: mark inbound connections by ingress interface so that
# replies exit the same interface via policy routing (fwmark -> table).
table inet mangle {
    chain prerouting {
        type filter hook prerouting priority mangle; policy accept;
        iif "eth1" ct mark set 0x1
{% if container_public_ip is defined %}
        iif "eth2" ct mark set 0x2
{% endif %}
    }

    chain output {
        type filter hook output priority mangle; policy accept;
        ct mark 0x1 meta mark set 0x1
{% if container_public_ip is defined %}
        ct mark 0x2 meta mark set 0x2
{% endif %}
    }
}
{% if container_public_ip is defined %}
{% set allowed_ports = [
    default_p2p_port | default(''),
    default_bootnode_p2p_port | default(''),
    default_bootnode_p2p_port_wss | default('')
] | reject('equalto', '') | unique | list %}

# Firewall: restrict inbound on eth2 (public) to specific P2P ports.
# eth0 (NAT) and eth1 (internal) remain unrestricted.
table inet filter {
    chain input {
        type filter hook input priority filter; policy accept;

        # Established/related and loopback always pass
        ct state established,related accept
        iif lo accept

        # ICMP/ICMPv6 needed for ping and IPv6 NDP
        iif "eth2" meta l4proto { icmp, ipv6-icmp } accept

{% if allowed_ports %}
        # Allowed public P2P ports
        iif "eth2" tcp dport { {{ allowed_ports | join(', ') }} } accept
{% endif %}

        # Drop everything else arriving on eth2
        iif "eth2" drop
    }
}
{% endif %}
